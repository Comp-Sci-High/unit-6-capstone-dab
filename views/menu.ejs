<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body> 

    <nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
    <div class="container-fluid">
    <a class="navbar-brand" href="home.html">Memento Coffee Shop</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="home.html">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="menu.ejs">Menu</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="aboutPage.html">About Page</a>
        </li>
        <li class="nav-item">
          <a class="nav-link disabled" aria-disabled="true">Disabled</a>
        </li>
      </ul>
    </div>
  </div>
</nav>

    <h1>Menu</h1>
    <br>

    <div class="Food-grid">
      <% for (let i = 0; i < Foods.length; i++) { %>
        <div class="Food-card">

          <img src="<%= Foods[i].image %>" alt="<%= Foods[i].foodName %>" />

          <div class="food">
            <h2><%= Foods[i].foodName %></h2>
            <p><strong>Ingredients:</strong> <%= Foods[i].indredients %></p>
            <p><strong>Price:</strong> <%= Foods[i].price %></p>
            <p><strong>hot:</strong> <%= Foods[i].hot %></p>
            <p><strong>calories:</strong> <%= Foods[i].calories %></p>
            <p><strong>rating:</strong> <%= Foods[i].rating %></p>
          </div>

        </div>
      <% } %>
    </div>

</body>
</html>